This repository contains the code used in the validation section of the HEAL paper. 

The data was first analyzed with HEAL. The scripts then load in the heal lists produced as an output of HEAL.

Reproducibility reports for HEAL runs can be found in the 'reproducibility_reports' directory. 